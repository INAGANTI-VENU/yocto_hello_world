# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-my-mod"
BBFILE_PATTERN_meta-my-mod = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-my-mod = "6"

LAYERDEPENDS_meta-my-mod = "core"
LAYERSERIES_COMPAT_meta-my-mod = "scarthgap"
